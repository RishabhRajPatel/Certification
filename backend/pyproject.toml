[project]
name = "maayad-backend"
version = "1.0.0"
description = "Maayad — Internship, offer letter & certificate management API (FastAPI)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py311"
